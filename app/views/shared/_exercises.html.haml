%table.table.table-striped
  %tbody#sortable1
    - exercises.each do |exercise|
      %tr
        %td= "#{exercise.repetitions} x"
        %td= "#{exercise.distance} meters"
        %td= exercise.stroke
        %td= "on #{exercise.interval} seconds"
